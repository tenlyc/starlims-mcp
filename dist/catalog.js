import { queryDatabaseSchema, databaseChangeSchema } from './query-database.js';
import * as z from 'zod/v4';
import { HOST_TOOL_CONTRACTS } from './host-contracts.js';
const allProfiles = ['unified', 'devtools', 'vscode-compat'];
const STARLIMSVSCODE_COMMIT = '92b9014244eb09a56ed589db5155c3b7914b70a2';
const upstreamProvenance = {
    repository: 'https://github.com/MrDoe/starlimsvscode',
    owner: 'MrDoe/starlimsvscode',
    license: 'MIT',
    relationship: 'derived-from-upstream',
    sourceCommit: STARLIMSVSCODE_COMMIT,
    note: 'Host-neutral contract derived from the reviewed starlimsvscode MCP implementation.'
};
const mcpProvenance = {
    repository: 'https://github.com/tenlyc/starlims-mcp',
    owner: 'tenlyc/starlims-mcp',
    license: 'MIT',
    relationship: 'original'
};
const shared = (id, title, description, risk, capability, inputSchema, provenance = upstreamProvenance) => ({ id, title, description, origin: provenance === upstreamProvenance ? 'starlimsvscode' : 'starlims-mcp', provenance, risk, capability, schemaVersion: '1.0', profiles: allProfiles, inputSchema });
const owned = (contract, provenance) => ({ ...contract, provenance });
const uri = z.string().min(1).describe('STARLIMS enterprise item URI.');
const language = z.string().optional().describe('Optional STARLIMS form language identifier.');
const maxItems = z.number().int().positive().max(10_000).optional();
const maxCharacters = z.number().int().positive().max(1_000_000).optional();
export const STARLIMS_TOOL_CATALOG = [
    ...HOST_TOOL_CONTRACTS,
    shared('browse_tree', 'Browse tree', 'Browse STARLIMS items below a folder URI or from the root.', 'read', 'items.browse', z.object({ uri: z.string().optional(), maxItems })),
    shared('search_by_name', 'Search by name', 'Search STARLIMS items by name.', 'read', 'items.search', z.object({ query: z.string().min(1), itemType: z.string().optional(), exactMatch: z.boolean().optional(), maxItems })),
    shared('global_code_search', 'Global code search', 'Search for text across STARLIMS code items.', 'read', 'code.search', z.object({ searchString: z.string().min(1), itemTypes: z.array(z.string()).optional(), maxItems })),
    shared('list_languages', 'List languages', 'List available STARLIMS form languages.', 'read', 'languages.list', z.object({ maxItems })),
    shared('get_item_code', 'Read item code', 'Read authoritative code for a STARLIMS item.', 'read', 'code.read', z.object({ uri, language, maxCharacters })),
    shared('get_form_resources', 'Read form resources', 'Read and parse the complete HTML/XFD Form Resources document for one explicit STARLIMS language. Use the Resources URI returned by browse or checkout tools. Resources XML has three distinct roles: ResourcesDataset/ResourcesTable is stored data; Resources/Resource/Id/Value is designer-paste data; Form/Resources/Data/KeyItem/TextItem is a loading binding and must never be submitted as resource data. Inspect formDiagnostics (binding status, GUID mismatch and missing column xtype) and runtimeVerified; a valid binding does not certify Designer or runtime compatibility. Build new Form XML from a Designer-generated same-control template and preserve typed column metadata.', 'read', 'forms.resources.read', z.object({ uri, language: z.string().min(1), includeXml: z.boolean().optional(), maxCharacters }), mcpProvenance),
    shared('read_log', 'Read log', 'Read STARLIMS server logs.', 'read', 'logs.read', z.object({ user: z.string().optional(), maxLines: z.number().int().positive().optional() })),
    shared('get_table_definition', 'Read table definition', 'Read a STARLIMS table XML definition.', 'read', 'tables.read', z.object({ uri, maxCharacters })),
    shared('checkout_item', 'Check out item', 'Check out an item before editing. HTML/XFD XML, CodeBehind, Resources and Guide share one form-family checkout. Check out once in the intended language; do not re-checkout children after editing because native re-checkout can overwrite working changes.', 'write', 'checkout.write', z.object({ uri, language })),
    shared('save_item', 'Save item', 'Save complete code to a checked-out STARLIMS item.', 'write', 'code.write', z.object({ uri, code: z.string(), language, expectedVersion: z.string().optional() })),
    shared('save_form_resources', 'Save form resources', 'Save HTML/XFD Form Resources for one explicit STARLIMS language and verify the source-control working copy by reading back. ResourceId matching is case-sensitive. A ResourcesDataset document is an explicit full replacement; designer-paste <Resources><Resource><Id>...</Id><Value>...</Value></Resource></Resources> input is merged while preserving existing GUIDs and server-only entries such as GUIDE. For HTML forms the host also verifies and repairs the standard Resources loading binding in Form XML using the enterprise GUID and the requested language; custom data sources are rejected for manual review. An already-open HTML Form Designer tab must be closed and reopened to reload its cached Resources grid; runtime synchronization occurs at check-in. Resources XML has three distinct roles: ResourcesDataset/ResourcesTable is stored data; Resources/Resource/Id/Value is designer-paste data; Form/Resources/Data/KeyItem/TextItem is a loading binding and must never be submitted as resource data. Inspect formDiagnostics (binding status, GUID mismatch and missing column xtype) and runtimeVerified; a valid binding does not certify Designer or runtime compatibility. Build new Form XML from a Designer-generated same-control template and preserve typed column metadata.', 'write', 'forms.resources.write', z.object({ uri, language: z.string().min(1), resourceXml: z.string().min(1), expectedVersion: z.string().optional() }), mcpProvenance),
    shared('set_form_resource', 'Set form resource', 'Create or update one ResourceValue by exact, case-sensitive ResourceId in an HTML/XFD form for one explicit STARLIMS language, preserving the remaining resource document. The source-control working copy and standard HTML Form Resources loading binding are verified after saving; reopen an already-open Designer form to refresh its Resources grid, and check in to synchronize runtime resources. Resources XML has three distinct roles: ResourcesDataset/ResourcesTable is stored data; Resources/Resource/Id/Value is designer-paste data; Form/Resources/Data/KeyItem/TextItem is a loading binding and must never be submitted as resource data. Inspect formDiagnostics (binding status, GUID mismatch and missing column xtype) and runtimeVerified; a valid binding does not certify Designer or runtime compatibility. Build new Form XML from a Designer-generated same-control template and preserve typed column metadata.', 'write', 'forms.resources.write', z.object({ uri, language: z.string().min(1), resourceId: z.string().min(1), resourceValue: z.string(), expectedVersion: z.string().optional() }), mcpProvenance),
    shared('checkin_item', 'Check in item', 'Check in a STARLIMS item after edits are complete.', 'write', 'checkout.checkin', z.object({ uri, reason: z.string().min(1), language })),
    shared('undo_checkout', 'Undo checkout', 'Undo checkout for a STARLIMS item.', 'destructive', 'checkout.undo', z.object({ uri })),
    owned({ id: 'query_database', title: 'Query database', description: 'Run a bounded parameterized SQL Server SELECT through the dedicated server-validated SELECT backend using the existing STARLIMS Database connection. No DataSource is saved. Only Database connection; no CTEs, comments, quoted identifiers or SQL literals. Use ? parameters. Requires SCM_API.McpQueryDatabase and the existing SQL Server Database connection; never fall back to scratch data sources.', origin: 'starlims-mcp', risk: 'read', capability: 'database.query', schemaVersion: '1.0', profiles: ['unified', 'devtools'], inputSchema: queryDatabaseSchema }, mcpProvenance),
    owned({ id: 'execute_database_change', title: 'Execute database change', description: 'Execute one parameterized INSERT VALUES, UPDATE SET or DELETE after mandatory human approval of this exact request. UPDATE/DELETE require simple AND filters. The backend counts matches in a serializable transaction before writing and rolls back if maxAffectedRows is exceeded. No automatic retries or scratch DataSource edits. Uses the existing STARLIMS SQL Server Database connection.', origin: 'starlims-mcp', risk: 'destructive', capability: 'database.change', schemaVersion: '1.0', profiles: ['unified', 'devtools'], inputSchema: databaseChangeSchema }, mcpProvenance),
    shared('execute_server_script', 'Execute server script', 'Execute a STARLIMS server script.', 'execute', 'scripts.execute', z.object({ uri, parameters: z.array(z.unknown()).optional(), outputType: z.enum(['ARRAY', 'JSON', 'XML']).optional(), entryPoint: z.string().optional(), maxCharacters })),
    shared('execute_data_source', 'Execute data source', 'Execute a STARLIMS data source.', 'execute', 'datasource.execute', z.object({ uri, parameters: z.array(z.unknown()).optional(), outputType: z.enum(['ARRAY', 'JSON', 'XML']).optional(), maxCharacters, maxRows: z.number().int().positive().optional() })),
    owned({ id: 'list_checked_out_items', title: 'List checked-out items', description: 'List checked-out STARLIMS items.', origin: 'starlims-mcp', risk: 'read', capability: 'checkout.list', schemaVersion: '1.0', profiles: ['unified', 'devtools'], inputSchema: z.object({ includeAllUsers: z.boolean().optional() }) }, mcpProvenance),
    owned({ id: 'query_checkin_history', title: 'Query check-in history', description: 'Query STARLIMS Source Control Manager history.', origin: 'starlims-mcp', risk: 'read', capability: 'scm.history', schemaVersion: '1.0', profiles: ['unified', 'devtools'], inputSchema: z.object({ user: z.string().min(1), dateFrom: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), dateTo: z.string().regex(/^\d{4}-\d{2}-\d{2}$/) }) }, mcpProvenance),
    owned({ id: 'refresh_checkout_tree', title: 'Refresh checkout tree', description: 'Refresh the starlimsvscode checked-out workspace mirror.', origin: 'starlimsvscode', risk: 'write', capability: 'checkout.refresh', schemaVersion: '1.0', profiles: ['unified', 'vscode-compat'], inputSchema: z.object({ includeAllUsers: z.boolean().optional() }) }, upstreamProvenance),
    owned({ id: 'vscode_save_local_item', title: 'Save local workspace item', description: 'Compatibility tool that saves a starlimsvscode local working-copy path.', origin: 'starlimsvscode', risk: 'write', capability: 'code.write.local', schemaVersion: '1.0', profiles: ['vscode-compat'], adapterTool: 'save_item', inputSchema: z.object({ localPath: z.string().min(1), language }) }, upstreamProvenance),
    owned({ id: 'create_item', title: 'Create item', description: 'Create a STARLIMS enterprise item.', origin: 'starlimsvscode', risk: 'write', capability: 'items.create', schemaVersion: '1.0', profiles: allProfiles, inputSchema: z.object({ itemName: z.string().min(1), itemType: z.string().min(1), language: z.string().min(1), categoryName: z.string().min(1), appName: z.string().min(1) }) }, upstreamProvenance),
    owned({ id: 'checkout_table', title: 'Check out table', description: 'Check out a STARLIMS table.', origin: 'starlimsvscode', risk: 'write', capability: 'tables.checkout', schemaVersion: '1.0', profiles: allProfiles, inputSchema: z.object({ uri }) }, upstreamProvenance),
    owned({ id: 'checkin_table', title: 'Check in table', description: 'Check in a STARLIMS table.', origin: 'starlimsvscode', risk: 'write', capability: 'tables.checkin', schemaVersion: '1.0', profiles: allProfiles, inputSchema: z.object({ uri, reason: z.string().min(1) }) }, upstreamProvenance),
    owned({ id: 'create_table', title: 'Create table', description: 'Create a STARLIMS database or dictionary table.', origin: 'starlimsvscode', risk: 'write', capability: 'tables.create', schemaVersion: '1.0', profiles: allProfiles, inputSchema: z.object({ tableName: z.string().min(1), dsn: z.string().min(1) }) }, upstreamProvenance),
    owned({ id: 'edit_table', title: 'Edit table', description: 'Save a full STARLIMS table XML definition.', origin: 'starlimsvscode', risk: 'write', capability: 'tables.write', schemaVersion: '1.0', profiles: allProfiles, inputSchema: z.object({ uri, tableXml: z.string(), expectedVersion: z.string().min(1).describe('Version from the complete get_table_definition read before editing.') }) }, upstreamProvenance),
    owned({ id: 'run_integration_tests', title: 'Run integration tests', description: 'Run host integration tests after explicit local approval.', origin: 'starlimsvscode', risk: 'execute', capability: 'tests.run', schemaVersion: '1.0', profiles: ['unified', 'vscode-compat'], inputSchema: z.object({ reason: z.string().optional(), maxCharacters }) }, upstreamProvenance),
    owned({ id: 'transfer_item_to_server', title: 'Transfer items to server', description: 'Transfer checked-out items to another configured STARLIMS server.', origin: 'starlimsvscode', risk: 'write', capability: 'transfer.run', schemaVersion: '1.0', profiles: ['unified', 'vscode-compat'], inputSchema: z.object({ targetServer: z.string().min(1), saveLocalEdits: z.boolean().optional() }) }, upstreamProvenance)
];
export function getProfileTools(profile, capabilities) {
    const supported = capabilities ? new Set(capabilities) : undefined;
    return STARLIMS_TOOL_CATALOG.filter((tool) => tool.profiles.includes(profile) && (!supported || supported.has(tool.capability)));
}
export function findToolContract(id) {
    return STARLIMS_TOOL_CATALOG.find((tool) => tool.id === id);
}
//# sourceMappingURL=catalog.js.map